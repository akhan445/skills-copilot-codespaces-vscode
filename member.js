function skillsMember() {
    var member = document.getElementById("member");
    var skill = document.getElementById("skill");
    var memberValue = member.value;
    var skillValue = skill.value;
    var memberArray = memberValue.split(",");
    var skillArray = skillValue.split(",");
    var memberArrayLength = memberArray.length;
    var skillArrayLength = skillArray.length;
    var memberSkill = document.getElementById("memberSkill");
    var memberSkillValue = "";
    var memberSkillArray = [];
    var memberSkillArrayLength = 0; 
}